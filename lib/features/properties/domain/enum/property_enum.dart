enum PropertyType {
  apartment,
  land,
  villa,
  agriculturalLand,
  industrialLand,
  farm,
  shop,
  architecture,
}

enum PropertyCategory { buy, rent, swap }

enum PropertyStatus { unCompleted, pending, rejected, active, unactive }

enum PropertyDeedType {
  green,
  courtRolling,
  municipal,
  industrial,
  agricultural,
}
